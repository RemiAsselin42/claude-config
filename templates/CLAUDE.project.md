# {{REPO_NAME}} — Project Notes

Generalized tooling rules (Graphify, RTK, MemPalace, `context/`, Think in Code) live in the global `~/.claude/CLAUDE.md` — do not duplicate them here.

Repo-specific pointers:

- Graph fallback when `graphify-out/` is missing locally (fresh clone, new machine): `~/.claude/vault/Projets/{{REPO_NAME}}/{{REPO_NAME}} - GRAPH_REPORT.md`
- MemPalace wing for this repo: `{{REPO_NAME}}`

Add below only what is specific to this repo and not derivable from the code: architecture decisions, hard constraints, build/test commands.

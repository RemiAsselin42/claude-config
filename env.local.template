# Copier ce fichier en env.local et remplir les valeurs pour cette machine
# env.local est gitignored — ne jamais committer ce fichier

export FIGMA_API_KEY="figd_..."
# Le vault Obsidian est dans vault/ à la racine du repo — pointer Obsidian directement là-dedans
